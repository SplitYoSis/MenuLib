Works on all versions, hooks with sysengine, a temporary library until sysengine gets a proper one.

```xml
<dependency>
  <groupId>com.octanepvp.splityosis</groupId>
  <artifactId>menulib</artifactId>
  <version>1.1.2</version>
</dependency>
```
